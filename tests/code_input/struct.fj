type MyStruct = struct {
    x: i32,
    y: i32,
    name: str,
}

fn main() {
    let data = make MyStruct {
        x = 1,
        y = 2,
        name = "hi"
    };
    let s = data.x + data.y;
    data.x += 10;
}
